for (int i = 0; i < xyz.length; i++) {
			xyz[i]=obj.next().charAt(0);
		}
		
		for (int i = 0; i < xyz.length; i++) {
			System.out.println("the char array "+i+" position value is : "+xyz[i]);
		}
		
		
		
		
		//int num=obj.nextInt();
		//int arr[]= {10,20,40,757,68};
//		int xyz[]; //declaration of array 
		//int []abc=new int[5];
		//abc[0]=55;
		for(int i=0;i<abc.length;i++)
		{
			abc[i]=obj.nextInt();
		}
		for(int i=0;i<abc.length;i++)
		{
			System.out.println("the value of array :"	+abc[i]);
		}
		//.out.println("the   oth position of array is :"+arr[0]);
		for (int i = 0; i < num; i++) {
			char ch=obj.next().charAt(0);	
		}